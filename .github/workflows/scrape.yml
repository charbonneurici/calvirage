name: Scraper Top 14 + Champions Cup + Challenge Cup

on:
  schedule:
    - cron: '0 6 * * 1'  # Lundi à 6h (après le week-end de matchs)
    - cron: '0 6 * * 4'  # Jeudi à 6h (matchs en semaine / reports)
  workflow_dispatch:       # Déclenchement manuel depuis GitHub

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright + Chromium
        run: npx playwright install chromium --with-deps

      - name: Run scraper Top 14 (LNR)
        run: node scripts/scrape-lnr.js

      - name: Run scraper F1
        run: node scripts/scrape-f1.js

      - name: Run scraper Champions Cup & Challenge Cup (EPCR)
        run: node scripts/scrape-epcr.js

      - name: Commit fixtures si changement
        run: |
          git config user.name "CalVirage Bot"
          git config user.email "bot@calvirage.fr"
          git add data/fixtures.json
          git diff --staged --quiet || git commit -m "chore: mise à jour fixtures $(date +%Y-%m-%d)"

      - name: Push
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
